import React from "react";
import workBoxSvg from '../assets/workbox.svg'; // Import the SVG file

const WorkBox = () => {
    return (
        <img src={workBoxSvg} alt="work-bubble" />
    );
}

export default WorkBox;
